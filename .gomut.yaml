# Generated by gomut init.
# Remove optional sections or values that are not needed for your project.
target:
  mode: package
  value: ./...
timeout: 10s
progress: auto
parallel: 0
jsonl: mutations.jsonl
html: report.html
sarif: report.sarif
type:
  - killed
  - lived
  - not-covered
  - timed-out
  - not-viable
kind:
  mode: standard
  enable: []
  disable: []
exclude:
  - "*.pb.go"
  - "*_mock.go"
  - internal/generated
isolation:
  copy_exclude:
    - tmp
    - internal/cache/**
baseline:
  input: baseline-in.jsonl
  output: baseline-out.jsonl
